namespace SFR.TemplateRandomizer.Parsers
{
    internal class DateRangeParser : IArgumentParser<(DateTimeOffset, DateTimeOffset)>
    {
        public (DateTimeOffset, DateTimeOffset) Parse(string input)
        {
            throw new NotImplementedException();
        }
    }
}
